# Live handoffs only

Only files present in this folder are live session handoffs.

Do **not** resurrect deleted handoffs from git history as authority.
Frontier decisions live in `docs/rse/wayfinder/` (map + tickets) and
`docs/rse/control/BOARD.md`.
