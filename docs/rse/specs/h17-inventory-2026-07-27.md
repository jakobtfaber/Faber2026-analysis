# h17 inventory — 2026-07-27 (owner-verified enumeration)

STATUS: STALE below — the owner reorganized h17 later on 2026-07-27; see
"Post-reorg layout" at the end. The original enumeration is kept for
provenance (it is the state the capture receipt's snapshots were taken
from).

Original status: VERIFIED (read-only enumeration on h17, depth-bounded
`find` over `~/` and `/data/`), recorded verbatim from the owner's session
report so the consolidation picture spans all machines, not only
jakob-mbp.

## `h17:/data/` — canonical data stores

- `/data/Faber2026` (20 GB) — the project data root:
  `data/{chime-frb, dsa-110}`, `evidence/` (RFI and bad-channel review
  experiments, 2026-07-21/22), `provenance/`.
- `/data/Faber2023_bursts` (31 GB) — full-Stokes pickles + notebooks per
  burst (27 items; predecessor-paper data).
- `/data/jfaber/` — scintillation sprint work: `sprint-cohdisc` (18 GB),
  `p1-window-upchan-20260714` (contains
  `scintillation-adjudication-20260715`,
  `subband-scintillation-20260715{,-repro}`),
  `upchan_crossacf_20260714{,_v2}` (286 MB each) + the two upchannelize
  scripts.
- Adjacent but a different project: `/data/dsa110-continuum*`,
  `/data/dsa110-contimg*`, etc.

## `h17:~/` — code, worktrees, runs, docs

- `~/Developer/repos/github.com/jakobtfaber/dsa110-FLITS` — the canonical
  FLITS clone (no Faber2026/analysis clone on h17; the manuscript lives at
  `~/papers/Faber2026`, 117 MB, with `main.tex`, `pipeline/`, `figures/`,
  `CONTEXT.md`).
- `~/Developer/worktrees/` — 15 FLITS worktrees: the controlled-fit series
  `dsa110-FLITS-controlled-{johndoeII,oran,zach}-2026072{2,3}[-v2..v4]`,
  plus `model-grid-diagnostic` and `resolution-diagnostic` (2026-07-22).
- `~/worktrees/` — 3 older worktrees: `flits-window-tuning`,
  `joint-tf-fits`, `t0audit-pr` (each with `scintillation/`,
  `analysis/chime-scintillation`, `flits/`).
- Run outputs: `~/flits-runs` (3.4 GB, incl. `data/scintleak`),
  `~/flits-controlled` (59 MB), `~/runs/faber2026_dm_power` (2.9 MB),
  `~/scratch/faber2026-fit-envelope-input-20260722` (fit-envelope packet
  v1/v2 + component-count audit).
- Harness/docs: `~/scint-injection-harness`, `~/FABER2026-SCINT-HANDOFF.md`
  (Jul 17), `~/OPERON-DRIVER.md` (Jul 17), stray figures
  `~/{casey,wilhelm}_fit_inputs.png`, `~/papers/Faber2025` (prior paper).

## Disposition

- h17 is the raw-data point of access (per the landed provenance docs) and
  holds the scint-redo step-1 frozen raw inputs; nothing here was touched by
  the 2026-07-27 branch consolidation, which covered only the jakob-mbp
  checkouts and the three GitHub remotes.
- The controlled-fit worktree sprawl (15 + 3 worktrees, many superseded
  v1–v3) and the two off-taxonomy trees (`~/flits-runs`,
  `~/flits-controlled`) are consolidation candidates, but per the
  retirement rules nothing on h17 is a deletion candidate without
  per-snapshot verification (path, branch/commit, dirty state, archive
  checksum) — a separately chartered lane when the owner opens it.

## Post-reorg layout (owner reorganization, 2026-07-27; verified read-only)

Owner's stated convention: all relevant work under
`h17:~/Developer/repos/github.com/jakobtfaber/Faber2026/`, all data
products under `h17:/data/Faber2026/`.

- `~/Developer/repos/github.com/jakobtfaber/Faber2026` — manuscript
  checkout, at the time of verification on branch
  `docs/clarify-chance-coincidence` (`7478397d0c36`) with an in-flight edit
  (`scripts/plot_ne2025_mw_properties.py` modified, untracked
  `scripts/ne2025_allsky_cache_nside32.npz`) — an active lane, preserved
  and not touched.
- `/data/Faber2026/` (42 GB): `data/`, `evidence/`, `provenance/`, `runs/`
  (now holds the former `~/flits-runs` and `~/flits-controlled`),
  `scratch/`. `/data` also gained `_holding/`, `_trash/`, and
  `_reorg-2026-07-27/` lanes.
- All 18 FLITS worktrees now live under `~/Developer/worktrees/` (the old
  `~/worktrees` three moved in; the registry was repaired — `git worktree
  list` is clean). Compatibility symlinks left behind: `~/worktrees`,
  `~/flits-runs`, `~/flits-controlled`, `~/scratch` →
  `~/Developer/scratch/misc`, `~/scint-injection-harness` →
  `~/Developer/scratch/scint-injection-harness`.
- Capture validity re-verified after the move: the three snapshot branches
  still match their worktrees exactly (joint-tf-fits 33/33 paths, both
  diagnostic worktrees `git diff <snapshot>` empty), so the capture
  receipt's verdicts carry over to the new paths.
- Still at pre-reorg state: `~/Developer/repos/github.com/jakobtfaber/dsa110-FLITS`
  `main` remains pre-rewrite history (`bad0ba49b049`); `~/papers/`
  and `~/runs/faber2026_dm_power` remain as small leftovers.
