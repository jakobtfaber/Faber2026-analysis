# Stub — hybrid control system (landed PR #59)

**Status:** superseded as executable plan (2026-07-19 docs scrub).

Machinery lives in `scripts/sync_state.py` + `docs/rse/control/program-state.toml`.
Lane table is **frozen / not the frontier** — use BOARD + wayfinder.
