{
  "version": 3,
  "created_at": "2026-07-15T17:49:36.300Z",
  "task_summary": "Switch FRB ToA definition from profile peak-bin to joint-fit model intrinsic centroid (t0)",
  "agents": [],
  "phases": [
    {
      "name": "Establish the model-t0 anchor mapping",
      "delegations": [
        {
          "name": "Anchor mapping",
          "steps": [
            {
              "title": "Confirm t0 semantics on the fit axis",
              "description": "Verify in burstfit.py that model t0 is the intrinsic (pre-scattering) Gaussian centroid on each band's io.py-processed time axis (ms), independent per band. Document how the axis relates to the raw cube (dt_ms, downsample factors, crop, _centre_burst roll) and confirm the roll is not recorded, so absolute time is only recoverable via the peak-relative route."
            },
            {
              "title": "Define the peak-relative scatter correction",
              "description": "For each band define scatter_correction = t0_model - t_ref, where t_ref is the profile-peak reference on the same fit axis. Decide t_ref rigorously: reconstruct the scattered model profile from fitted params and take its argmax, and cross-check against the io.py data-profile peak to confirm the model reproduces the observed peak to sub-sample. This correction is anchor-free and rides on top of the existing absolute peak-bin ToA."
            },
            {
              "title": "Validate on one burst (chromatica)",
              "description": "For chromatica, evaluate the fitted model both bands, extract t0_C/t0_D from the joint_fit JSON, compute scattered-peak times and the per-band scatter correction, and sanity-check magnitudes against tau/alpha expectation (CHIME correction >> DSA correction). Confirm the differential correction has the sign and scale that scattering physics predicts."
            }
          ]
        }
      ],
      "id": "phase-0"
    },
    {
      "name": "Compute model ToAs for all 12 bursts",
      "delegations": [
        {
          "name": "Correction table",
          "steps": [
            {
              "title": "Batch the scatter correction over 12 bursts",
              "description": "Loop all 12 joint_fit JSONs: extract t0_C, t0_D and posteriors, reconstruct scattered-peak per band, compute per-band scatter correction and its posterior-propagated uncertainty (from t0 err_minus/err_plus). Handle the bespoke/superseded fits (zach C2D4, johndoeII C2D2) per their canonical source."
            },
            {
              "title": "Assemble the correction table",
              "description": "Build a per-burst CSV: t0_C, t0_D, scattered-peak_C, scattered-peak_D, scatter_correction_C, scatter_correction_D, differential (C-D), plus posterior localization errors per band. Save as an artifact."
            },
            {
              "title": "Prototype the new offsets",
              "description": "Compute model-based measured_offset_ms = peak-bin offset + (scatter_correction_C - scatter_correction_D) for all 12, and tabulate against the current peak-bin offsets to quantify the shift and how it correlates with tau/alpha."
            }
          ]
        }
      ],
      "id": "phase-1"
    },
    {
      "name": "Wire the switch into the crossmatch pipeline",
      "delegations": [
        {
          "name": "Pipeline + errors",
          "steps": [
            {
              "title": "Feed model t0 into compute_toa",
              "description": "In crossmatching, add a code path that takes the model-based per-band ToA (peak-bin ToA + scatter correction) and refers it to 400 MHz via compute_toa, replacing the profile-peak input while keeping the DM referral and geometric-delay machinery intact."
            },
            {
              "title": "Rebuild the error budget",
              "description": "Replace/augment error_chime_ms and error_dsa_ms so the ToA error combines the existing DM-uncertainty referral term with the new posterior-derived localization term (t0 err from the joint fit) in quadrature. Preserve combined_dm_uncertainty_ms semantics or add a clearly-named new field."
            },
            {
              "title": "Regenerate toa_crossmatch_results.json",
              "description": "Regenerate the results JSON with model-based measured_offset_ms and the new error terms for all 12 bursts, keeping the schema backward-compatible where downstream plotting reads it."
            }
          ]
        }
      ],
      "id": "phase-2"
    },
    {
      "name": "Update guards, deliverables, and methods text",
      "delegations": [
        {
          "name": "Tests + writeup",
          "steps": [
            {
              "title": "Update golden-JSON and alignment tests",
              "description": "Update test_crossmatching_notebook_reproduction.py field-by-field asserts and the notebook_reproduction_fixture to the new convention; check test_codetection_data.py peak-shift alignment and test_association.py still hold or update them. Run the crossmatch/association test subset to green."
            },
            {
              "title": "Build the comparison deliverables",
              "description": "Produce the peak-bin-vs-model-t0 comparison figure and finalize the correction CSV as manuscript-facing artifacts, with the differential scatter shift annotated per burst."
            },
            {
              "title": "Draft the ToA methods subsection",
              "description": "Write the manuscript methods text: current peak convention, why the scattered-profile peak biases the CHIME-DSA offset (band-dependent tau), the intrinsic-centroid t0 definition, the peak-relative anchor, and the two-term error budget."
            }
          ]
        }
      ],
      "id": "phase-3"
    }
  ],
  "desired_outputs": [
    "Per-burst model-based scatter-correction table (CHIME t0, DSA t0, profile-peak, scatter correction per band, differential correction) as CSV",
    "Updated toa_crossmatch_results.json with model-based measured_offset_ms and a posterior-derived localization error term",
    "Comparison figure/table: peak-bin offsets vs model-t0 offsets for all 12 bursts, with the differential scatter shift called out",
    "Updated tests (test_crossmatching_notebook_reproduction.py and any golden-JSON asserts) reflecting the new convention",
    "Manuscript methods subsection text on the ToA definition (peak vs model t0, scatter-bias argument, error budget)"
  ],
  "feasibility": {
    "confidence": "high",
    "rationale": "The joint fits already store per-band intrinsic centroid t0 with full posteriors, and the model evaluator can reconstruct the scattered profile, so the scatter correction is directly computable. The main risk is the absolute-time anchor: the _centre_burst roll in io.py is applied in place without recorded provenance, so the switch must be built as a peak-relative correction layered on the existing anchor rather than a from-scratch absolute recompute — which the plan does. Touching the golden toa_crossmatch_results.json requires coordinated test updates."
  }
}