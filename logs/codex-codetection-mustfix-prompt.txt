Implement the four MUST-FIX items from the Fable figure review (logs/codex-fable-codetection-review.txt) for Faber2026 codetection observation prototypes.

## Workspace
- Pipeline code (edit here): /Users/jakobfaber/Developer/repos/github.com/jakobtfaber/Faber2026/pipeline/
- Key files:
  - flits/batch/codetection_data.py (load, TOA align, subburst window, SUBBURST_PAD_MS=3.0)
  - flits/batch/codetection_plots.py (plot_codetection, plot_codetection_observations)
  - flits/batch/codetection_joint.py (crop_band_dict)
  - analysis/scattering-refit-2026-06/batch_codetection_data.py (batch regen driver)
- TOA crossmatch: crossmatching/toa_crossmatch_results.json
- Output prototypes: ../figures/prototypes/codetection/ (relative to pipeline/)
- Data: FLITS_DATA=~/Data/Faber2026/dsa110

## Current behavior (do not regress)
- Connected CHIME + hatched freq gap + DSA waterfall; time profile top, spectrum right
- Raw data only; per_band_scale=True on waterfalls
- Center time on midpoint between first/last sub-burst peak TOAs after applying CHIME shift = -measured_offset_ms
- Left crop bound: first peak TOA - 3 ms; right: last band half-max trailing edge + 3 ms
- White hatched gap; grey RFI markers; vertical band labels on grey strips in left margin

## Known diagnostics (verify/fix)
```
mahi   xlim ~(-6, 37)  peaks DSA -8.7 / CHIME +8.7  — huge trailing window, burst at edge
oran   xlim ~(-6, 29)   peaks ±3.3                   — long trailing dead space
phineas xlim ~(-15, 21) peaks ±12.4                  — two-tone DSA snippet boundary ~-6 ms
whitney xlim weird; peaks ±13.5                      — CHIME two-tone boundary; verify crop
isha/oran/mahi — CHIME counterpart faint in waterfall/spectrum marginal
```

## MUST-FIX tasks

### 1. TOA alignment audit
- Trace load_codetection_bands: shift_c = -toa_offset_ms(burst) applied to CHIME only.
- Verify sign/convention against crossmatching/toa_crossmatch_results.json for phineas, whitney, mahi (large peak separations after centering may be real DM/residual delay OR a bug).
- Document alignment semantics in codetection_data.py module docstring: CHIME shifted by crossmatch offset; time axis centered on midpoint between sub-burst peaks (residual separation preserved intentionally).
- If you find a clear sign/application bug, fix it. Do NOT force both peaks to t=0 unless audit proves crossmatch is wrong — separated sub-bursts are expected when DM differs.

### 2. Enforce 3 ms padding (mahi, oran, phineas, whitney)
- Root issue likely: band_onpulse_span(last) extends through long scattering tails or noise, inflating right edge.
- Fix subburst window logic so padding is exactly SUBBURST_PAD_MS beyond:
  - left: first sub-burst peak TOA
  - right: last sub-burst peak TOA + trailing extent capped reasonably (e.g. half-max OR fixed ms after peak, whichever is shorter; avoid 30+ ms tails from noise)
- After fix, verify for all 12 bursts: left_pad = peak_first - xlim[0] ≈ 3 ms and right_pad = xlim[1] - trailing_reference ≈ 3 ms (within ~0.2 ms), and total window not dominated by empty noise (target: casey-like ~8–12 ms width unless bursts are widely separated).

### 3. Unequal snippet lengths (phineas, whitney)
- Before final crop, compute temporal overlap where BOTH bands have finite/non-zero data (or valid samples).
- Trim shared window to overlap; OR mark no-data time within a band's imshow extent with white+hatch (reuse gap hatch style) when one snippet ends early — do not leave flat zero-fill texture boundaries.
- Implement in codetection_data and/or codetection_plots.

### 4. Faint CHIME counterparts (isha, oran, mahi)
- Waterfalls already use per_band_scale; ensure CHIME is visible when co-detection exists.
- Fix spectrum marginal: plot per-band-normalized spectra (match per_band_scale intent) so DSA doesn't dwarf CHIME.
- Optionally improve time-profile marginal: per-band normalized profiles overlaid (black + crimson or two colors) instead of raw sum hiding faint band — if minimal change, at least fix spectrum panel.

## Verification
Run from pipeline/:
```
FLITS_DATA=~/Data/Faber2026/dsa110 conda run -n flits python analysis/scattering-refit-2026-06/batch_codetection_data.py ../figures/prototypes/codetection
```
Then a quick Python check printing xlim and peaks for all 12 bursts.

Do NOT commit. Leave a short summary of changes and before/after for mahi, oran, phineas, whitney, isha.
